# Release Notes

## v3.2.0

- Add JobId-based dedupe with `drop` and `replace` behavior.
- Add `cancelByJobId` for pending and delayed tasks.
- Persist and restore JobId dedupe index for queued tasks.
- Document JobId dedupe usage in README files.
